# Environment Variables

# Server (not exposed to client)
PORT=5173

# Client (exposed via VITE_ prefix)
VITE_API_URL=http://localhost:3000
VITE_APP_NAME=React 2026 Template
VITE_ENABLE_DEBUGGING=false
